.conteiner
  =render partial: "sheard/header"
  .main-conteiner
    = form_for @group do |f|
      .class-head
        .class-form
          %ul.class-name-fild
            %li.input-class
              %label.class-name-label
                グリープ名を入力してください
            %li.input-class
              = f.text_field :name, class: 'class-name-input', placeholder: '(例)３−４組'
          .chat-group-form__field.clearfix
            / この部分はインクリメンタルサーチ（ユーザー追加の非同期化)のときに使用します
          .chat-group-form__field.clearfix
            .chat-group-form__field--left
              = f.label "チャットメンバー", class: "chat-group-form__label"
            .chat-group-form__field--right
              / グループ作成機能の追加時はここにcollection_check_boxesの記述を入れてください
              = f.collection_check_boxes :user_ids, @users, :id, :nickname
              / この部分はインクリメンタルサーチ（ユーザー追加の非同期化)のときにも使用します
          .chat-group-form__field.clearfix
            .chat-group-form__field--left
            .chat-group-form__field--right
              = f.submit class: 'chat-group-form__action-btn'

            
            


