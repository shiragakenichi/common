.conteiner
  = render partial: "sheard/header"
  = link_to group_messages_path(@group.id) do
    グループのメッセージ画面に戻る
  = @album.name
  = link_to edit_group_album_path(@group.id,@album.id) do
    アルバムを編集する
  .album-photo
    -if @album.images.length != 0
      - @album.images.each do |photo|
        = link_to group_album_photo_path(@group.id,@album.id,photo.id) do
          = image_tag photo.image.url, class:"item-image",height:'200px'
    - else
      アルバムに写真が登録されていません
  